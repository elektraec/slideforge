{
  "name": "slideforge",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "runtime": "node scripts/build-runtime.mjs && node scripts/sync-presentation-runtimes.mjs",
    "predev": "npm run runtime",
    "dev": "vite --host 0.0.0.0",
    "build": "npm run runtime && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "dompurify": "^3.4.15",
    "esbuild": "^0.28.2",
    "jszip": "^3.10.2",
    "katex": "^0.18.7",
    "marked": "^18.0.13",
    "mermaid": "^11.17.2",
    "reveal.js": "^6.0.2",
    "vite": "^8.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
